#include <core/structs.h>

/*
 * trim
 *   Trims the 3' and 5'-ends of a profile
 *
 * @args profile_struct* profile
 *   A profile handler struct
 * @args args_p_struct* arguments
 *   An arguments handler struct
*/
void trim(profile_struct* profile, args_p_struct* arguments);
